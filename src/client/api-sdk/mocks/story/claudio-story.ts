import Story from "../../models/Story";

const mockClaudioStory = new Story();
mockClaudioStory.id = 2;
mockClaudioStory.author = 2;
mockClaudioStory.comments = [1, 2];
mockClaudioStory.content = 'Que onda que pez abcd lorem ipsum.';

export default mockClaudioStory;